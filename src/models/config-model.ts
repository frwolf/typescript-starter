/**
 * Represents a configuration object.
 * @property {string} id - Unique identifier for the configuration.
 * @property {string} description - Description of the configuration.
 * @property {number} count - Numerical value associated with the configuration.
 */
export interface Config {
  id: string;
  description: string;
  count: number;
}
